# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# App Configuration
# For production/Vercel: set to your canonical HTTPS URL (e.g. https://biblenow.io)
# so Sign In and app links use the correct domain instead of the preview URL.
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=BibleNOW

# Optional: service role (server-side only; never expose in client)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Email (e.g. Resend)
# RESEND_API_KEY=your_resend_api_key
# NEXT_PUBLIC_RESEND_FROM_EMAIL=no-reply@yourdomain.com

# API
# NEXT_PUBLIC_API_BASE_URL=https://your-api.example.com/api

# Jitsi / streaming (if used)
# NEXT_PUBLIC_JITSI_SERVER_URL=https://your-stream.example.com
# NEXT_PUBLIC_JITSI_DOMAIN=your-stream.example.com

# Stripe (use test keys for development)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# hCaptcha (for auth sign-in/sign-up)
# NEXT_PUBLIC_HCAPTCHA_SITEKEY=your_hcaptcha_site_key

# Auth providers: add keys in .env.local (never commit real keys)
# NEXT_PUBLIC_APPLE_CLIENT_ID=...
# FIREBASE_* / GOOGLE_* etc.

# JWT / server-side streaming (if used)
# JITSI_JWT_APP_ID=...
# JITSI_JWT_SECRET=...
# JITSI_SUBJECT=...
# JITSI_DOMAIN=...
